% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multinomial.R
\name{estimate_categ_func_data_multinomial_parallel}
\alias{estimate_categ_func_data_multinomial_parallel}
\title{Parallel estimation of Z and p using multinomial link GAM}
\usage{
estimate_categ_func_data_multinomial_parallel(
  time_points,
  w_mat,
  n_basis = 25,
  method = "ML"
)
}
\arguments{
\item{time_points}{A numeric vector of time points}

\item{w_mat}{A matrix of categorical values (time × individuals)}

\item{n_basis}{Number of basis functions for smoothing (default = 25)}

\item{method}{GAM fitting method (default = "ML")}
}
\value{
A named list with latent Z estimates and probability estimates
}
\description{
Parallel version of multinomial estimation for improved performance
with large datasets.
}
\details{
This is the parallel implementation of the multinomial approach.
It uses the \code{foreach} package with \code{\%dorng\%} for reproducible
parallel computation.

Requires a parallel backend to be registered (e.g., via \code{doParallel})
for actual parallel execution.
}
\examples{
\dontrun{
# Setup parallel backend
library(doParallel)
cl <- makeCluster(2)
registerDoParallel(cl)

# Generate sample data
set.seed(123)
n_time <- 50
n_individuals <- 20
time_points <- seq(0, 1, length.out = n_time)
w_mat <- matrix(sample(1:3, n_time * n_individuals, replace = TRUE),
                nrow = n_time, ncol = n_individuals)

# Parallel estimation
result <- estimate_categ_func_data_multinomial_parallel(time_points, w_mat)

# Cleanup
stopCluster(cl)
}

}
